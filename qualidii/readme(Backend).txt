In order to start the backend, you must start the java file that says "TdpApplication.java"

You can find it in src->main->com->QualitestGroup->QualiDii->TdpApplication

I do this through intelliJ, but if that is not an option, then I suggest starting it through the terminal.